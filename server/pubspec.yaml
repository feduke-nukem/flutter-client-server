name: server
description: An new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shared:
    path: ../shared
  database:
    path: packages/database
  grpc: ^3.2.4
  hotreloader: ^4.2.0

dev_dependencies:
  mocktail: 1.0.0
  test: ^1.19.2
  lints: ">=2.0.0 <4.0.0"
  build_runner: 2.4.8

generator client {
  provider = "dart run orm"
  output   = "../lib/src/prisma_client_generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id   Int    @id @default(autoincrement())
  name String

  @@map("users")
}

name: user_app_flutter
description: A new Flutter project with Serverpod.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  bloc: ^8.1.3
  collection: ^1.18.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  serverpod_flutter: 1.2.5
  shimmer: ^3.0.0
  user_app_client:
    path: ../user_app_client

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
